{
  "name": "interpreter-data",
  "version": "0.1.0",
  "private": true,
  "description": "1891 Interpreter system of record on Cloudflare D1 (ADR-001) — strangler migration off Google Sheets.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema:local": "wrangler d1 execute interpreter-data --local --file=./schema.sql",
    "schema:remote": "wrangler d1 execute interpreter-data --file=./schema.sql",
    "tail": "wrangler tail interpreter-data"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.78.0"
  }
}
